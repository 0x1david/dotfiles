# ~/.bashrc
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '


alias b='cd ..'
alias vim='nvim'

eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa
clear
